services:
  - type: web
    name: fairshare-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm ci && npm run build -w @splitwise/api
    startCommand: npm run start -w @splitwise/api
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CLIENT_ORIGIN
        sync: false
